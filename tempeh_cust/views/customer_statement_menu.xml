<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- Action on contacts / Follow-up customer list:
             select customers -> Action -> Print Customer Statements -->
        <record id="action_open_customer_statement_wizard_server" model="ir.actions.server">
            <field name="name">Print Customer Statements</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="binding_model_id" ref="base.model_res_partner"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code">
if records:
    action = {
        'name': '客戶月結單',
        'type': 'ir.actions.act_window',
        'res_model': 'customer.statement.wizard',
        'view_mode': 'form',
        'target': 'new',
        'context': {'default_partner_ids': [(6, 0, records.ids)]},
    }
            </field>
        </record>

        <!-- Expose the native Follow-up Reports (催款報表) screen in Accounting → Customers.
             Use the working account_report client action (the followup module's own
             action_account_followup uses the unregistered 'account_report_followup' tag). -->
        <record id="action_followup_report_tempeh" model="ir.actions.client">
            <field name="name">Follow-up Reports</field>
            <field name="tag">account_report</field>
            <field name="context" eval="{'report_id': ref('account_reports.followup_report')}"/>
        </record>

        <menuitem id="menu_account_followup_report"
                  name="Follow-up Reports"
                  parent="account.menu_finance_receivables"
                  action="action_followup_report_tempeh"
                  groups="account.group_account_invoice"
                  sequence="11"/>
    </data>
</odoo>
